namespace PoViEmu.Core.Machine.Ops
{
    public enum Modifier
    {
        Unknown = 0,

        @byte,
        dword,
        far,
        qword,
        @short,
        to,
        tword,
        word
    }
}